// Стандартни (пресетни) категории разходи — по добрата практика на производствени
// и търговски фирми. Централен списък; използва се за бързо създаване на липсващи
// категории и за групиране в справките. Стойностите са имена (както се пазят в
// ExpenseCategory.name), за да няма промяна по модела.

export const EXPENSE_PRESETS: string[] = [
  "Материали",
  "Суровини",
  "Транспорт",
  "Гориво",
  "Външни услуги",
  "Машини и оборудване",
  "Персонал",
  "Реклама и маркетинг",
  "Наем",
  "Електроенергия",
  "Вода",
  "Амортизации",
  "Комунални услуги",
  "Данъци и такси",
  "Други",
];

/** Кои пресети липсват сред вече наличните имена (case-insensitive). */
export function missingPresets(existingNames: string[]): string[] {
  const have = new Set(existingNames.map((n) => n.trim().toLowerCase()));
  return EXPENSE_PRESETS.filter((p) => !have.has(p.toLowerCase()));
}
